{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "save_meta_v1",
  "title": "SaveMeta",
  "description": "Persistent per-save status metadata for the load menu (saves/meta.json).",
  "type": "object",
  "required": ["entries"],
  "additionalProperties": false,
  "properties": {
    "entries": {
      "type": "object",
      "description": "Map of save file stem (MD5 hash, no extension) to its metadata.",
      "additionalProperties": {
        "$ref": "#/definitions/SaveEntryMeta"
      }
    }
  },
  "definitions": {
    "SaveEntryMeta": {
      "type": "object",
      "required": ["status"],
      "additionalProperties": false,
      "properties": {
        "status": {
          "type": "string",
          "enum": ["ok", "hash_mismatch", "corrupt"],
          "description": "ok: loaded successfully and hash matches. hash_mismatch: file was modified after saving (tamper detected). corrupt: failed to parse or initialize."
        }
      }
    }
  }
}
